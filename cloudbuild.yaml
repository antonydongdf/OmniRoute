steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/omniroute'
      - '.'
    env:
      - 'DOCKER_BUILDKIT=1'
images:
  - 'gcr.io/$PROJECT_ID/omniroute'

# Enable BuildKit for Cloud Run deployment
